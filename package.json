{
  "name": "kata",
  "version": "1.0.0",
  "description": "AI agent programming best-practices framework with reusable workflows, skills, constraints, and validation loops",
  "bin": {
    "kata": "scripts/kata.js",
    "skills": "scripts/skills.js"
  },
  "scripts": {
    "install:skill": "node scripts/install.js add kata",
    "init": "node scripts/init-project.js",
    "test": "node --check scripts/install.js && node --check scripts/init-project.js && node --check scripts/kata.js && node --check scripts/skills.js"
  },
  "keywords": [
    "codex",
    "claude-code",
    "skill",
    "agent",
    "workflow",
    "project-memory"
  ],
  "author": "User",
  "license": "MIT"
}
